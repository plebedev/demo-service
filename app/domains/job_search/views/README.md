# Job Search Views

The registered view definitions are assembled in `register.py` and materialized
by perspective builders in `perspectives/`. Views return generic
`PerspectiveView` structures with `ViewSection` and `EvidenceLink` records.
